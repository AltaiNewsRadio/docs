# Quickstart

## 1. Create an API key (admin)

```bash
POST /createApiKey
x-admin-key: <admin-secret>

2. Call a read-only endpoint

curl -H "x-api-key: aie_live_XXXX" \
https://us-central1-anrtestmap.cloudfunctions.net/getSystemReadiness

3. Call an assessment endpoint (with scope)

Ensure your key has the assessments scope.


---

### `authentication.mdx`
```mdx
# Authentication

All protected endpoints require an API key.

x-api-key: aie_live_XXXXXXXX


## Admin authentication

Admin-only endpoints require:

x-admin-key: <admin-secret>


Admin keys are stored as Firebase secrets and can be rotated at any time.